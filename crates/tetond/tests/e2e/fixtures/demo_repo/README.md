# demo

A tiny fixture repo the Teton Code acceptance suite drives a session against.
It has a public library, a binary, and a `secrets/` directory used to exercise
the `local-only` privacy boundary (BR-1).
